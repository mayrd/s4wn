# Unit Textures — Mayans

## Purpose
Diffuse texture maps for **unit** models.

## What is expected here
Sub-files: `unit_{unitKey}.png` (e.g. `unit_worker.png`, `unit_soldier.png`). 1024x1024 RGBA PNGs.

## How assets are consumed
Referenced from `nation.json -> units.{unit}.texture`.
