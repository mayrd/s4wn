# Shared Textures — Mayans

## Purpose
Common texture maps used across both units and buildings in this nation pack.

## What is expected here
May contain `.gitkeep` if no shared textures are needed. These are textures referenced by multiple asset types, not tied to a single building or unit.

## How assets are consumed
Referenced from `nation.json` wherever a shared texture path is used.
