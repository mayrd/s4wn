# Decoration Models — Romans

## Purpose
Nation-specific decorative 3D props (border posts, flags, etc.).

## What is expected here
Sub-files: `borderpost.obj` / `borderpost.mtl` — the territory border marker placed by Pioneer settlers. May also contain `flag_{lowid}.glb` — the nation's animated flag model.

## How assets are consumed
Referenced by `nation.json -> visuals.decorations.borderPost` and `visuals.flag`.
